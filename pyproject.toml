[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "hawk"
version = "1.0.0"
description = ""
authors = [
    { name = "Your Name", email = "your@email.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
    "maturin>=1.9.4",
]
